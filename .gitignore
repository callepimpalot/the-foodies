# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist

# Recipe-import pipeline artifacts (large, already imported to Supabase)
data/*.json
dist-ssr
*.local
.env
build

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.code-workspace

# Overnight status page — generated, served locally, not source
landing/status.html
# Overnight working files — agent scratch, not source
OVERNIGHT-BRIEF.md
MORNING-BRIEF.md
